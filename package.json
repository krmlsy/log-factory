{
  "name": "js-log-lib",
  "version": "1.0.0",
  "description": "Log library for projects",
  "repository": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "rm -rf dist/lib && npx tsc && npx tsc --build tsconfig.es5.json"
  },
  "author": "Kerem Ulusoy",
  "license": "MIT",
  "devDependencies": {
    "tslog": "^4.8.2",
    "typescript": "^4.9.5",
    "util": "^0.12.5"
  }
}
